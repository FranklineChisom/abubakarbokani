'use client';

import React from 'react';

// This component is currently unused but kept to prevent import errors
const SEO: React.FC<{ children?: React.ReactNode }> = ({ children }) => {
  return <>{children}</>;
};

export default SEO;